{
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp apple-touch-icon.png dist/apple-touch-icon.png && sed -i \"s|%%GEMINI_KEY%%|${GEMINI_KEY}|g\" dist/index.html && sed -i \"s|%%CLIENT_ID%%|${CLIENT_ID}|g\" dist/index.html"
  }
}
